services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: a02_crypto_web
    ports:
      - "8002:80"
    volumes:
      - .:/var/www/html
    depends_on:
      db:
        condition: service_healthy
    environment:
      - DB_HOST=db
      - DB_NAME=ecommerce_db
      - DB_USER=root
      - DB_PASSWORD=rootpassword
    networks:
      - a02_crypto_network

  db:
    image: mysql:8.0
    container_name: a02_crypto_db
    ports:
      - "3302:3306"
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: ecommerce_db
    volumes:
      - db_data:/var/lib/mysql
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    networks:
      - a02_crypto_network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-prootpassword"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  db_data:

networks:
  a02_crypto_network:
    driver: bridge

