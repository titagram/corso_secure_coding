# VULNERABILITÀ CRITICA: File environment esposto pubblicamente!
# Questo file contiene credenziali sensibili e NON dovrebbe essere accessibile via web
# Nota: In produzione questo file si chiamerebbe .env e dovrebbe essere fuori dalla document root

# Database Configuration
DB_HOST=db
DB_NAME=document_system
DB_USER=root
DB_PASSWORD=rootpassword

# Application Configuration
APP_NAME=Document Management System
APP_ENV=production
APP_DEBUG=true
APP_URL=http://localhost:8005

# API Keys (VULNERABILI!)
API_KEY=sk_live_1234567890abcdefghijklmnop
SECRET_KEY=my_super_secret_key_12345_do_not_share
JWT_SECRET=jwt_secret_key_for_tokens_2024

# Email Configuration
SMTP_HOST=smtp.example.com
SMTP_USER=admin@example.com
SMTP_PASSWORD=smtp_password_12345
SMTP_PORT=587

# File Upload Configuration
MAX_FILE_SIZE=10485760
UPLOAD_PATH=/var/www/html/uploads
ALLOWED_EXTENSIONS=pdf,doc,docx,txt,jpg,png

# Session Configuration
SESSION_LIFETIME=7200
SESSION_ENCRYPTION_KEY=session_encryption_key_12345

