version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: broken_auth_web
    ports:
      - "8000:80"
    volumes:
      - .:/var/www/html
    depends_on:
      - db
    environment:
      - DB_HOST=db
      - DB_NAME=invoicing_app
      - DB_USER=root
      - DB_PASSWORD=rootpassword
    networks:
      - broken_auth_network

  db:
    image: mysql:8.0
    container_name: broken_auth_db
    ports:
      - "3310:3306"
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: invoicing_app
    volumes:
      - db_data:/var/lib/mysql
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    networks:
      - broken_auth_network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-prootpassword"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  db_data:

networks:
  broken_auth_network:
    driver: bridge


